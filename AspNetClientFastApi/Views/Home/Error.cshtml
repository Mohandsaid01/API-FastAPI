@{
    ViewData["Title"] = "Erreur";
    var message = ViewData["ErrorMessage"] as string;
}

<div class="container text-center mt-5">
    <h1 class="display-4 text-danger fw-bold">Une erreur est survenue</h1>
    <p class="lead">@message</p>
    <a href="@Url.Action("Index", "Actors")" class="btn btn-primary mt-3">Retour à l'accueil</a>
</div>
