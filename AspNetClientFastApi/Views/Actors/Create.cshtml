@model AspNetClientFastApi.Models.Actor

@{
    ViewData["Title"] = "Ajouter un acteur";
}

<div class="container mt-5">
    <h2 class="text-center mb-4">Ajouter un acteur 🎭</h2>

    <form asp-action="Create" method="post" class="mx-auto" style="max-width: 600px;">
        <div class="mb-3">
            <label asp-for="Id" class="form-label">ID</label>
            <input asp-for="Id" class="form-control" placeholder="Entrez l'ID unique" />
        </div>

        <div class="mb-3">
            <label asp-for="Name" class="form-label">Nom</label>
            <input asp-for="Name" class="form-control" placeholder="Nom de l'acteur" />
        </div>

        <div class="mb-3">
            <label asp-for="Bio" class="form-label">Biographie</label>
            <textarea asp-for="Bio" class="form-control" rows="3" placeholder="Petite description"></textarea>
        </div>

        <div class="mb-3">
            <label asp-for="Picture" class="form-label">Image URL</label>
            <input asp-for="Picture" class="form-control" placeholder="Lien de l'image" />
        </div>

        <button type="submit" class="btn btn-success px-4">Créer</button>
        <a asp-action="Index" class="btn btn-outline-secondary ms-2">Retour</a>
    </form>
</div>
